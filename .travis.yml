language: php

php:
  - 7.1

before_script:
  - cp .env.travis .env
  - touch database/database.sqlite
  - composer self-update
  - composer install --no-interaction
  - php artisan migrate
  - php artisan serve &

script:
  - vendor/bin/phpunit
  - php artisan dusk